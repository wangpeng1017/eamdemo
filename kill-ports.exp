#!/usr/bin/expect -f
set timeout 60
set password "xx198910170014Z"
set server "root@8.130.182.148"

puts "\n强制释放端口 3001..."
spawn ssh $server "fuser -k 3001/tcp 2>/dev/null || true; lsof -nP -i:3000 -i:3001 || true"
expect "password:" { send "$password\r" }
expect eof
