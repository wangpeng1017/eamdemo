# 委托单→检测任务→任务报告→客户报告 全流程问题分析报告

> 生成时间：2026-02-23
> 分析范围：完整业务链路 API + 数据模型 + 前端触发逻辑

---

## 一、流程概览

```
委托单 (Entrustment)
    ↓ entrustmentId 关联
检测任务 (TestTask)
    ↓ 任务完成 → 生成任务报告
任务报告 (TestReport)
    ↓ 整合多个任务报告 → 生成客户报告
客户报告 (ClientReport)
    ↓ 审批流程 → 导出 PDF/Word
```

---

## 二、发现的 BUG 清单

### 🔴 严重问题（直接导致功能失败）

---

#### BUG-01：任务报告生成 — entrustmentId 字段类型不匹配导致查询失败

**文件**：`src/app/api/report/generate/route.ts:147`

**问题**：
```typescript
entrustment = await prisma.entrustment.findUnique({
  where: { id: task.entrustmentId },  // ❌ 可能存的是 entrustmentNo 而非 id
})
```

`TestTask.entrustmentId` 在 Schema 中是 `String? @db.VarChar(50)`，没有 Prisma 外键关联声明。
`task.entrustmentId` 可能存储的是 `WT20260223XXXX` 格式的编号而非 cuid，导致 `findUnique({ where: { id: ... } })` 永远查不到。

**影响**：委托单信息（clientName、sampleNo 等）无法带入任务报告，字段全为 null。

---

#### BUG-02：任务报告生成 — 编号格式三套不统一

**文件**：
- `src/app/api/report/generate/route.ts:188` → 格式：`RPT-YYYYMMDD-XXX`（3位）
- `src/app/api/test-report/route.ts:37` → 格式：`BGYYYYMMDDNNNN`（BG前缀是客户报告的）
- `src/lib/generate-no.ts:15` → 定义的正式前缀是 `RWBG`，格式：`RWBGYYYYMMDDNNNN`

**问题**：三套编号并存，数据库里任务报告的 reportNo 格式不统一，无法按编号规律排序和查询。

---

#### BUG-03：任务报告生成 — badRequest/notFound 没有 return，后续代码继续执行

**文件**：`src/app/api/report/generate/route.ts:87-92`

```typescript
try {
  const body = await request.json()
  taskId = body.taskId
  // ...
} catch (error) {
  badRequest('请求参数格式错误')  // ❌ 没有 return！
}

if (!taskId) {
  badRequest('缺少任务ID')  // ❌ 没有 return！
}
// 代码继续执行，taskId 仍为空字符串 ''，传给 prisma 会导致崩溃
```

**影响**：这是任务报告生成失败的最直接原因。当请求体解析失败或 taskId 为空时，代码继续执行，最终在数据库查询阶段崩溃报 500。

---

#### BUG-04：数据权限过滤将 TestTask 当 Entrustment 链路处理，检测人看不到自己的任务

**文件**：`src/app/api/task/all/route.ts:16-17`

```typescript
const permissionFilter = await getEntrustmentBasedFilter(user.id)
// 返回 { entrustmentId: { in: [只有该用户创建/跟单的委托单ID] } }
const where: any = { ...permissionFilter }
```

`TestTask` 没有 `createdById` 字段，只有 `assignedToId`。
`getEntrustmentBasedFilter` 基于委托单创建人/跟单人过滤，对于检测人员（assignedToId）不是委托单关系人的情况，过滤结果为空数组。

**影响**：检测人员在"生成报告"弹窗中选任务时，下拉列表为空，无法选择任何任务，整个生成入口失效。

---

### 🟡 中等问题（功能残缺或数据错误）

---

#### BUG-05：任务报告生成 — sheetData 列顺序硬编码，模板不一致时数据错位

**文件**：`src/app/api/report/generate/route.ts:33-37`

```typescript
// 假设固定列序：0=检测项目，1=技术要求，2=实测值，3=判定，4=备注
const parameter = row[0]?.v || ...
const standard   = row[1]?.v || ...
const value      = row[2]?.v || ...
```

不同业务的 Fortune-sheet 模板列顺序可能不同，没有任何列头验证逻辑。

---

#### BUG-06：客户报告生成 — testStandards 字段来源错误，检测依据永远为空

**文件**：`src/app/api/client-report/generate/route.ts:63-70`

```typescript
tasks.forEach(task => {
  task.testData?.forEach((data: any) => {
    if (data.standard && !testStandards.includes(data.standard)) {
      testStandards.push(data.standard)  // ❌ TestData.standard 是"技术要求值"，不是"检测标准名"
    }
  })
})
```

`TestData.standard` 字段存储的是技术要求（如 `≤0.1%`），而不是检测标准名称。
真正的检测标准名称在 `EntrustmentProject.method` 字段，但代码没有读取。

---

#### BUG-07：客户报告 generate 接口 — 无完整错误处理，body 字段未校验

**文件**：`src/app/api/client-report/generate/route.ts:13-28`

```typescript
const body = await request.json()  // 没有 try-catch
const { entrustmentId, taskIds, ... } = body
// taskIds 直接用于 { id: { in: taskIds } }，若为 undefined 则查询崩溃
```

---

#### BUG-08：客户报告导出 Word — task 可能为 null，导出内容使用默认硬编码数据

**文件**：`src/app/api/report/client/[id]/export/docx/route.ts:61-66`

```typescript
const firstTaskReport = report.tasks?.[0]
const task = firstTaskReport?.task || null  // task 可能为 null

const data = prepareClientReportData(task, entrustment, sample, report)
// task 为 null 时，results 返回默认的 QCT 6项（Pb/Hg/Cd/Cr6+/PBBs/PBDEs）
```

当没有关联任务报告时，导出的 Word 里检测结果全是错误的默认占位数据。

---

#### BUG-09：docx-renderer.ts — 硬编码 QCT 禁限用物质场景，不适用其他业务

**文件**：`src/lib/docx-renderer.ts:188-229`

```typescript
const defaultItems = [
  { testItem: 'Pb', standardReq: '≤0.1%' },
  { testItem: 'Hg', standardReq: '≤0.1%' },
  { testItem: 'Cd', standardReq: '≤0.01%' },
  ...
]
```

硬编码了汽车零部件有害物质检测场景，对力学性能、电性能等其他检测场景完全错误。

---

### 🟢 低风险问题（逻辑隐患）

---

#### BUG-10：generate-no.ts 依赖 sys_number_counter 表，此表可能不存在于 Prisma Schema

**文件**：`src/lib/generate-no.ts:57-70`

`sys_number_counter` 表通过原生 SQL 操作，但 Prisma Schema 中没有对应 model 定义。
若表未建立，走降级方案（count 查询），高并发下会产生重复编号。

---

#### BUG-11：/api/test-report POST 接口编号前缀使用 BG（客户报告前缀）

**文件**：`src/app/api/test-report/route.ts:36-37`

```typescript
const reportNo = `BG${today}${String(count + 1).padStart(4, '0')}`
// ❌ BG 是客户报告前缀，任务报告应用 RWBG
```

---

## 三、修复优先级建议

| 优先级 | BUG | 原因 |
|--------|-----|------|
| P0 | BUG-03 | badRequest 无 return，生成必崩 |
| P0 | BUG-04 | 检测人看不到任务，生成入口失效 |
| P1 | BUG-01 | entrustmentId 关联错，报告信息缺失 |
| P1 | BUG-06 | 客户报告检测依据永远为空 |
| P2 | BUG-02/11 | 编号格式统一 |
| P2 | BUG-07 | 补充错误处理 |
| P3 | BUG-05/08/09 | 数据提取健壮性 |
| P3 | BUG-10 | 编号生成并发安全 |

---

## 四、数据模型关系说明

```
Entrustment (委托单)
  ├── EntrustmentProject[] (检测项目，含 method 字段=检测标准)
  └── Sample[] (样品)
        └── TestTask[] (检测任务，注意 entrustmentId 无外键)
              ├── TestData[] (检测数据，standard=技术要求值，非标准名)
              └── TestReport[] (任务报告)
                    └── ClientReport (客户报告，通过 clientReportId 关联)
```

---

*报告由 Claude Code 自动生成，仅供开发参考*
