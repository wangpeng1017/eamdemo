# 设备管理、耗材管理、外包管理、财务管理、体系文件 BUG 分析报告

> 生成时间：2026-02-23
> 分析范围：5 个业务模块 API + 数据模型

---

## 一、模块概览

| 模块 | API 路径 | 主要数据模型 |
|------|----------|------------|
| 设备管理 | `/api/device/*` | Device, DeviceMaintenance, DeviceRepair, DeviceCalibration |
| 耗材管理 | `/api/consumable/*`, `/api/consumable-transaction` | Consumable, ConsumableTransaction |
| 外包管理 | `/api/outsource-order/*` | OutsourceOrder, Supplier |
| 财务管理 | `/api/finance/*` | FinanceReceivable, FinancePayment, FinanceInvoice |
| 体系文件 | `/api/system-document/*` | SystemDocument |

---

## 二、发现的 BUG 清单

### 🔴 严重问题（直接导致功能失败或数据错误）

---

#### BUG-D01：设备管理 — notFound/badRequest 无 return，代码继续执行（与 BUG-03 同类）

**文件**：`src/app/api/device/[id]/route.ts:15-16`、`:46`

```typescript
// GET 获取详情
if (!device) {
  notFound('设备不存在')  // ❌ 没有 return！
}
return success(device)  // device 为 null 时执行到这里会 crash
```

```typescript
// DELETE 删除检查
if (taskCount > 0) {
  badRequest(`无法删除：该设备有 ${taskCount} 个关联检测任务`)  // ❌ 没有 return！
}
await prisma.device.delete({ where: { id } })  // 即使有关联任务也会执行删除！
```

**影响**：
- GET：设备不存在时，接口返回 `success(null)` 而非 404
- DELETE：有关联检测任务时仍执行删除，造成数据孤立（检测任务的 deviceId 悬空）

---

#### BUG-D02：维修记录删除 — 先删再检查，状态恢复逻辑在删除后执行

**文件**：`src/app/api/device/repair/[id]/route.ts:92-108`

```typescript
// ❌ 先执行删除
await prisma.deviceRepair.delete({ where: { id } })

// 再查询该设备其他未完成维修数
const pendingRepairs = await prisma.deviceRepair.count({
  where: { deviceId: repair.deviceId, status: { not: 'completed' } },
})

// 基于删除后的数量决定是否恢复设备状态
if (pendingRepairs === 0) {
  await prisma.device.update({ data: { status: 'Running' } })
}
```

逻辑本身没有问题（先删再查其他未完成的记录是正确的），但是缺少事务包裹。如果删除后的更新设备状态操作失败，数据库状态将不一致（维修记录已删、设备仍是 Maintenance 状态）。

**影响**：数据库状态不一致，设备状态永久卡在「维护中」。

---

#### BUG-D03：外包管理 — OutsourceOrder 没有 `@id` 主键声明

**文件**：`prisma/schema.prisma:954`

```prisma
model OutsourceOrder {
  orderNo      String    @unique @db.VarChar(50)   // ❌ 只有 @unique，没有 @id
  supplierId   String?
  ...
}
```

Prisma 要求每个 model 必须有 `@id` 字段。这会导致：
1. `prisma db push` 会报错或产生不正确的表结构
2. `prisma.outsourceOrder.findUnique()` 调用时若传入 `id` 字段会直接报 TypeScript 类型错误
3. 级联删除、关联查询可能出现异常

API 代码已通过 `{ where: { orderNo: id } }` 绕过，但底层模型定义有缺陷。

**影响**：Schema 定义不合规，潜在的数据库迁移和 ORM 查询问题。

---

#### BUG-D04：外包管理 — filter=my 时覆盖了权限过滤条件，导致 `where.task` 被替换

**文件**：`src/app/api/outsource-order/route.ts:17-41`

```typescript
// 第一步：权限过滤
const permissionFilter = await getEntrustmentBasedFilter(user.id)
if (Object.keys(permissionFilter).length > 0) {
  where.task = permissionFilter  // 设置 where.task
}

// 第二步：filter=my 时
if (filter === 'my') {
  where.task = {                 // ❌ 完全覆盖了上面的 where.task！
    project: { subcontractAssignee: user.id }
  }
}
```

当 `filter === 'my'` 时，数据权限过滤（`where.task = permissionFilter`）被完全替换，普通用户可能看到不属于自己权限范围内的外包订单。

**影响**：数据权限绕过，存在安全风险。

---

#### BUG-D05：财务管理 — 应收账款权限过滤直接展开到顶层，与 FinanceReceivable 字段不匹配

**文件**：`src/app/api/finance/receivable/route.ts:17`、`src/app/api/finance/invoice/route.ts:18`

```typescript
// getEntrustmentBasedFilter 返回 { entrustmentId: { in: [...ids] } }
const permissionFilter = await getEntrustmentBasedFilter(user.id)
const where: Record<string, unknown> = { ...permissionFilter }
// 展开后：where.entrustmentId = { in: [...ids] }
```

`FinanceReceivable` 确实有 `entrustmentId` 字段，所以查询本身不会报错，但逻辑存在问题：
- 权限基于委托单创建人/跟单人过滤，财务人员（非委托单关联人）的 `permissionFilter` 会返回空数组
- 结果：财务人员看不到任何应收账款，但他们应该有全局查看权限

**影响**：财务角色的用户无法查看应收账款和发票列表（返回空）。

---

### 🟡 中等问题（功能残缺或数据错误）

---

#### BUG-D06：耗材管理 — 出库库存判断使用 `Number()` 转换，未处理 Decimal 精度问题

**文件**：`src/app/api/consumable-transaction/route.ts:88-92`

```typescript
const currentStock = Number(consumable!.stockQuantity)  // Decimal → Number 可能丢失精度

if (validated.type === 'out' && validated.quantity > currentStock) {
  badRequest(`库存不足`)  // ❌ badRequest 没有 return！（同 BUG-03 同类问题）
}
```

两个问题：
1. `Number(Decimal)` 在极端大数值时可能丢失精度
2. `badRequest()` 没有 return，继续执行会用负库存创建出库记录

**影响**：可能出现库存为负的情况。

---

#### BUG-D07：耗材管理 — 库存状态计算逻辑错误，status=0 含义不统一

**文件**：`src/app/api/consumable-transaction/route.ts:129-134`

```typescript
let status = 1 // 正常
if (newStock === 0) {
  status = 0   // ❌ 注释说 0=缺货，但在 consumable/route.ts POST 里 status=0 代表「禁用」
} else if (...) {
  status = 2   // 低库存
}
```

`Consumable.status` 字段含义在不同接口中不一致：
- `consumable/route.ts`：`status = 1 正常, 0 禁用`（创建时 `parseInt(data.status)`）
- `consumable-transaction/route.ts`：`status = 1 正常, 0 缺货, 2 低库存`

**影响**：前端状态显示混乱，缺货商品可能被误认为「禁用」。

---

#### BUG-D08：设备管理 — 设备编号生成存在并发重复风险

**文件**：`src/app/api/device/route.ts:27-30`

```typescript
const count = await prisma.device.count({
  where: { deviceNo: { startsWith: `SB${today}` } }
})
const deviceNo = `SB${today}${String(count + 1).padStart(4, '0')}`
```

与之前报告中 BUG-10 相同的模式：使用 `count + 1` 生成编号，并发时多个请求拿到同一个 count 值，导致编号重复（重复编号在 `@unique` 约束下会抛出 500 错误）。

**影响**：高并发场景下设备创建失败。

---

#### BUG-D09：财务管理 — 发票创建时 paymentDate 校验后仍允许传 null

**文件**：`src/app/api/finance/invoice/route.ts:72-74`

```typescript
if (!data.paymentDate) {
  return error('PAYMENT_DATE_REQUIRED', '回款日期为必填项')
}
// ...
paymentDate: data.paymentDate || null,  // ❌ 已经校验必填，这里 || null 是多余的防御但逻辑自洽
```

逻辑上没有直接错误，但 `issuedDate` （开票日期）没有做校验，允许为空（开票时不填开票日期语义上不合理）。

---

#### BUG-D10：财务管理 — 发票唯一性检查有竞态条件，并发下可能重复关联

**文件**：`src/app/api/finance/invoice/route.ts:77-84`

```typescript
// 检查委托单是否已被关联
const existing = await prisma.financeInvoice.findFirst({
  where: { entrustmentId: data.entrustmentId }
})
if (existing) {
  return error('ENTRUSTMENT_ALREADY_LINKED', '该委托单已关联发票')
}

// ❌ 检查与创建不在同一事务中！
const result = await prisma.$transaction(async (tx) => {
  const invoice = await tx.financeInvoice.create(...)  // 可能重复创建
})
```

两个并发请求同时通过「唯一性检查」后，都会进入事务并创建发票，绕过了重复关联的限制。

**影响**：同一委托单可能关联多张发票。

---

#### BUG-D11：外包管理 — `filter=my` 过滤依赖 `EntrustmentProject.subcontractAssignee` 字段，该字段可能不存在

**文件**：`src/app/api/outsource-order/route.ts:35-41`

```typescript
if (filter === 'my') {
  where.task = {
    project: {
      subcontractAssignee: user.id   // 依赖 EntrustmentProject.subcontractAssignee 字段
    }
  }
}
```

需要确认 `EntrustmentProject` model 中是否存在 `subcontractAssignee` 字段。若字段不存在，Prisma 会报 TypeScript 类型错误或运行时报错。

---

#### BUG-D12：设备管理 — 维修记录 PATCH 完成时不更新 `updatedAt`（DeviceRepair 无 `updatedAt` 自动更新问题）

DeviceRepair 有 `updatedAt DateTime @updatedAt`，Prisma 会自动更新，此处无问题。但维修完成时只更新 `DeviceRepair` 的状态，设备本身的 `nextCalibrationDate`、`lastCalibrationDate`、`operatingHours` 等不会联动更新。

**影响**：设备台账数据不会因维修完成而自动更新，需要手动维护。

---

### 🟢 低风险问题（逻辑隐患）

---

#### BUG-D13：体系文件 — 删除操作无权限校验，任何登录用户可删除所有文件

**文件**：`src/app/api/system-document/[id]/route.ts:39-44`

```typescript
export const DELETE = withAuth(async (request, user, context) => {
  const { id } = await context!.params as { id: string }
  await prisma.systemDocument.delete({ where: { id } })  // ❌ 无角色/权限检查
  return success({ deleted: true })
})
```

仅校验了「是否登录」，未校验用户是否有「管理员」或「文件管理员」角色。任何已登录用户都可以删除体系文件。

---

#### BUG-D14：体系文件 — 更新操作不校验记录是否存在，Prisma 会抛出 P2025 错误

**文件**：`src/app/api/system-document/[id]/route.ts:21-35`

```typescript
const doc = await prisma.systemDocument.update({
  where: { id },
  data: { ... },
})
// ❌ 如果 id 不存在，Prisma 抛出 PrismaClientKnownRequestError P2025
// 该错误会被 withAuth 的错误处理捕获，但返回的是通用 500，而非 404
```

---

#### BUG-D15：耗材管理 — 保养计划接口使用 `withErrorHandler` 而非 `withAuth`，无需登录

**文件**：`src/app/api/device/maintenance-plan/route.ts:3`、`src/app/api/device/calibration-plan/route.ts:3`

```typescript
export const GET = withErrorHandler(async (request: NextRequest) => {  // ❌ 不需要登录！
```

保养计划、定检计划接口使用 `withErrorHandler` 而非 `withAuth`，任何人无需登录即可查询或创建保养计划记录。

---

## 三、修复优先级建议

| 优先级 | BUG | 原因 |
|--------|-----|------|
| P0 | BUG-D01 | notFound/badRequest 无 return，删除操作穿透保护逻辑 |
| P0 | BUG-D04 | filter=my 覆盖权限过滤，存在数据安全风险 |
| P0 | BUG-D05 | 财务角色看不到任何应收账款 |
| P1 | BUG-D02 | 维修删除缺少事务，状态不一致 |
| P1 | BUG-D06 | 出库库存检查后无 return，可能产生负库存 |
| P1 | BUG-D07 | 耗材 status 字段含义冲突 |
| P1 | BUG-D10 | 发票唯一性竞态，可能重复关联 |
| P2 | BUG-D03 | OutsourceOrder 缺 @id 定义 |
| P2 | BUG-D08 | 设备编号并发重复 |
| P2 | BUG-D13 | 体系文件删除无权限校验 |
| P3 | BUG-D11 | filter=my 字段依赖待确认 |
| P3 | BUG-D14 | 更新不存在记录返回 500 而非 404 |
| P3 | BUG-D15 | 设备计划接口无需登录 |

---

## 四、跨模块共性问题总结

以下问题在多个模块普遍存在，建议统一修复：

| 问题类型 | 涉及模块 | 修复方案 |
|----------|----------|----------|
| `notFound`/`badRequest` 无 return | 设备、耗材 | 在 `api-handler.ts` 中让这两个函数直接 throw，而非返回 Response |
| 权限过滤器不区分角色 | 外包、财务 | `getEntrustmentBasedFilter` 增加角色判断，财务角色返回空过滤（全量） |
| 编号生成并发安全 | 设备 | 统一使用 `generateNo()` + `sys_number_counter` 表，废弃 `count + 1` 方案 |
| 操作无权限校验 | 体系文件 | 关键写操作增加角色校验（检查 `user.roles` 或 `user.roleCode`） |

---

## 五、数据模型问题汇总

```
Device (设备)
  ├── 无 createdById：无法过滤"我的设备"
  ├── status 使用英文字符串（Running/Maintenance/Idle/Scrapped），前端需同步
  └── DeviceMaintenance/DeviceCalibration 接口无认证

OutsourceOrder (外包订单)
  └── 无 @id 主键声明（严重 Schema 缺陷）

Consumable (耗材)
  └── status 字段含义：创建接口(0=禁用) vs 出库接口(0=缺货) 不统一

FinanceReceivable (应收账款)
  └── clientId 字段存在但创建时未写入（POST 未传 clientId）

SystemDocument (体系文件)
  └── 无 createdById：无法追溯文件上传人，无法做权限隔离
```

---

*报告由 Claude Code 自动生成，仅供开发参考*
